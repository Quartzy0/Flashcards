<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/textView"
        android:layout_width="90dp"
        android:layout_height="32dp"
        android:text="Add collection"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/collection_name_input"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:ems="10"
        android:inputType="textPersonName"
        app:layout_constraintTop_toBottomOf="@id/textView"
        app:layout_constraintLeft_toLeftOf="@id/textView"
        android:hint="Collection name" />
    <EditText
        android:id="@+id/collection_description_input"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:ems="10"
        android:gravity="start|bottom"
        android:inputType="textMultiLine"
        app:layout_constraintTop_toBottomOf="@id/collection_name_input"
        app:layout_constraintLeft_toLeftOf="@id/collection_name_input"
        android:hint="Collection description (optional)" />
</androidx.constraintlayout.widget.ConstraintLayout>